GA Notes

Nao adicionei encoding porque todas as variaveis sao int64 ou float64 segundo o .ipynb compartilhado.
Apenas a coluna 'Outcome' é categórica (que é a target). Preciso fazer encoding da variável target?
- Claude said No

Balanceamento do dataframe em relacao a variavel target:
- 65% No
- 35% Yes

Analise das variaveis:
- Pregnancies parece ok
- [!!!] Glucose 0 parece erro
- [!!!] BloodPressure 0 parece erro. Abaixo de 40 tmb.
- [!!!] SkinThickness 0 parece erro.
- Insulin: Normal (jejum): geralmente entre 2 e 25 µU/mL (alguns laboratórios usam limites um pouco diferentes, como 3–25 ou 5–20)
- [!!!] BMI 0 parece erro.
- DiabetesPedigreeFunction e Age parecem ok.

O .ipynb indica que nao há valores faltantes para nenhuma das variaveis. A presenca de valores estranhos de "0" sugere
uma imputação ineficiente de NAs por 0s.

O pipeline de modelagem está configurado. Agora vou adicionar os diferentes tipos de modelos que foram usados no
no notebook de exemplo:
- Random Forest Classifier
- Logistic Regression
- KNN
- Support Vector Classifier
- Decision Tree Classifier
- AdaBoost Classifier
- Gradient Boosting Classifier
- XGBoost Classifier
- LightGBM Classifier

As metricas de desempenho foram adicionadas para refletir o notebook:
- accuracy
- recall
- precision
- f1_macro
- roc_auc
- n_samples (usado pelo Donald)

PROXIMOS PASSOS
# Ao inves de dropar os zeros, o notebook usou KNN = 5 como estrategia de inputacao.
# Tratamento de outliers
# Otimizacao de hiperparametros



Escolha do melhor modelo
- Criação do pipeline de inferencia
- 9 modelos foram treinados e depois o melhor foi escolhido segundo o maior roc_auc no split 'validate'

# Por algum motivo, o código passou a falhar e só rodar para 3 classes de modelos.
